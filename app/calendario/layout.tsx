// Forzar rendering dinámico para esta ruta (usa useSearchParams)
export const dynamic = 'force-dynamic'

export default function CalendarioLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
